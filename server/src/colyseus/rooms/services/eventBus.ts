// interface IEventBus {
//   broadcastUpdatedGameState: () => void;
//   broadcastNotification: ({type, message, triggeredBy} : {type: string, message: string, triggeredBy: string}) => void;
// }

// export default class EventBus implements IEventBus {

// }